#include <Mesh.h>

Mesh::~Mesh() {
	// TODO
	/*
	if (VertexBuffer) {
		VertexBuffer->Release();
		VertexBuffer = nullptr;
	}
	
	if (IndexBuffer != nullptr) {
		IndexBuffer->Release();
		IndexBuffer = nullptr;
	}
	*/
}
